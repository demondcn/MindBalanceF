{
  "name": "mindbalancef",
  "private": true,
  "workspaces": [
    "frontend",
    "Backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace mindbalance-backend\" \"npm run dev --workspace frontend -- --host 0.0.0.0\"",
    "build": "npm run build --workspace frontend",
    "test": "npm run test --workspace frontend && npm run test --workspace mindbalance-backend",
    "test:coverage": "npm run test:coverage --workspace frontend && npm run test:coverage --workspace mindbalance-backend",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "concurrently": "^9.2.1",
    "playwright": "^1.54.2"
  }
}
